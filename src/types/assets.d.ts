// Ambient declarations for non-code side-effect / asset imports so the
// TypeScript language server resolves them even before Next.js generates types.
declare module "*.css";
declare module "*.scss";
